import styled from "styled-components";
import { Column } from "../../globalStyles";

export const JobScopes = styled.ul`
	margin-left: 3rem;
`;

export const JobList = styled.li`
	margin-bottom: 1em;
`;